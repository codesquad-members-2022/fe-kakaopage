import { renderer } from '../renderer.js';

const book = {
  home: (DATA) => {},
  series: (DATA) => {},
  bestseller: (DATA) => {},
  ranking: (DATA) => {},
  thriller: (DATA) => {},
  selfimprovement: (DATA) => {},
};

export { book };
