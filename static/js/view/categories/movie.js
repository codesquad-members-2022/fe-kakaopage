import { renderer } from '../renderer.js';

const movie = {
  home: (DATA) => {},
  ranking: (DATA) => {},
  action: (DATA) => {},
  animation: (DATA) => {},
  all: (DATA) => {},
};

export { movie };
