import { renderer } from '../../renderer.js';

const webnovel = {
  home: (DATA) => {},
  weeky: (DATA) => {},
  fantasy: (DATA) => {},
  romance: (DATA) => {},
  romanceFantasy: (DATA) => {},
  wuxia: (DATA) => {},
  fantasyDrama: (DATA) => {},
  bl: (DATA) => {},
};

export { webnovel };
