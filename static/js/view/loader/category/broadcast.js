import { renderer } from '../../renderer.js';

const broadcast = {
  home: (DATA) => {},
  varietyshow: (DATA) => {},
  drama: (DATA) => {},
  animation: (DATA) => {},
  foreign: (DATA) => {},
  all: (DATA) => {},
};

export { broadcast };
