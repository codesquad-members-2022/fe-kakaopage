const ContentsHeader = (title) => {
  return `<div class="contents-header">
            <h3 class="contents-title">${title}</h3>
            <a href="#">더보기</a>
          </div>`;
};

export default ContentsHeader;